{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9007,
            "pathMappings": {
                "/var/www/bae-api": "${workspaceFolder}"
            }
        }
    ]
}